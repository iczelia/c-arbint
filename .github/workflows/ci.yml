name: CI

on:
  push:
  pull_request:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read
  packages: write

jobs:
  package:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install packaging dependencies
        run: |
          sudo apt -o Acquire::Retries=5 update
          sudo apt -o Acquire::Retries=5 install -y autoconf automake libtool pkg-config xz-utils

      - name: Bootstrap, configure, and build dist tarball
        id: dist
        run: |
          ./bootstrap
          ./configure --enable-lto=auto
          make -j"$(nproc)" dist-xz
          dist_tarball="$(ls -t c-arbint-*.tar.xz | head -n1)"
          echo "dist_tarball=$dist_tarball" >> "$GITHUB_OUTPUT"

      - name: Upload dist artifact
        uses: actions/upload-artifact@v4
        with:
          name: c-arbint-dist
          path: ${{ steps.dist.outputs.dist_tarball }}
          if-no-files-found: error

  build-ubuntu:
    runs-on: ${{ matrix.runner }}
    needs: package
    strategy:
      fail-fast: false
      matrix:
        arch: [x64, arm64]
        compiler: [gcc, clang, tcc]
        lto: ['on', 'off']
        tune: ['on', 'off']
        exclude:
          - compiler: tcc
            lto: 'on'
          - compiler: tcc
            tune: 'on'
        include:
          - arch: x64
            runner: ubuntu-24.04
          - arch: arm64
            runner: ubuntu-24.04-arm
    name: build-ubuntu (${{ matrix.arch }}, ${{ matrix.compiler }}, lto-${{ matrix.lto }}, tune-${{ matrix.tune }})

    steps:
      - name: Install build dependencies
        run: |
          sudo apt -o Acquire::Retries=5 update
          sudo apt -o Acquire::Retries=5 install -y build-essential pkg-config clang tcc

      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: c-arbint-dist
          path: dist

      - name: Extract, build, and test
        run: |
          export CC="${{ matrix.compiler }}"

          configure_args=""
          if [ "${{ matrix.lto }}" = "on" ]; then
            configure_args="$configure_args --enable-lto=yes"
          else
            configure_args="$configure_args --enable-lto=no"
          fi

          if [ "${{ matrix.tune }}" = "on" ]; then
            configure_args="$configure_args --with-base-optimization=tuned"
          else
            configure_args="$configure_args --with-base-optimization=default"
          fi

          tarball="$(ls dist/*.tar.xz | head -n1)"
          srcdir="$(tar -tf "$tarball" 2>/dev/null | head -n1 | cut -d/ -f1)"
          tar -xf "$tarball" -C dist
          cd "dist/$srcdir"
          ./configure $configure_args
          make -j"$(nproc)"
          make check
          echo "=== Contents of src/.libs ==="
          ls -la src/.libs/ || true

      - name: Copy artifacts to upload directory
        if: matrix.compiler != 'tcc' && matrix.tune == 'off'
        run: |
          mkdir -p artifacts
          find dist -name "*.so" -o -name "*.so.*" | xargs -I {} cp {} artifacts/ || true
          ls -la artifacts/

      - name: Upload shared library artifacts
        if: matrix.compiler != 'tcc' && matrix.tune == 'off'
        uses: actions/upload-artifact@v4
        with:
          name: build-linux-${{ matrix.arch == 'x64' && 'x86-64' || matrix.arch == 'arm64' && 'arm64' || matrix.arch }}-${{ matrix.compiler }}-lto${{ matrix.lto }}
          path: artifacts/*
          if-no-files-found: warn

  build-docker:
    runs-on: ubuntu-24.04
    needs: package
    strategy:
      fail-fast: false
      matrix:
        distro: [fedora, alpine]
        compiler: [gcc, clang]
        lto: ['on', 'off']
        tune: ['on', 'off']
    name: build-docker (${{ matrix.distro }}, ${{ matrix.compiler }}, lto-${{ matrix.lto }}, tune-${{ matrix.tune }})

    steps:
      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: c-arbint-dist
          path: dist

      - name: Build and test in Docker
        run: |
          docker run --rm \
            -e DISTRO="${{ matrix.distro }}" \
            -e CC="${{ matrix.compiler }}" \
            -e MATRIX_LTO="${{ matrix.lto }}" \
            -e MATRIX_TUNE="${{ matrix.tune }}" \
            -v "$PWD/dist:/work/dist" \
            -w /work \
            "${{ matrix.distro }}:latest" \
            sh -eu -c '
              if [ "$DISTRO" = "fedora" ]; then
                dnf -y install gcc clang make pkgconf-pkg-config tar xz groff
              elif [ "$DISTRO" = "alpine" ]; then
                apk add --no-cache build-base clang pkgconf tar xz groff
              else
                echo "unsupported distro: $DISTRO" >&2
                exit 1
              fi

              configure_args=""
              if [ "$MATRIX_LTO" = "on" ]; then
                configure_args="$configure_args --enable-lto=yes"
              else
                configure_args="$configure_args --enable-lto=no"
              fi

              if [ "$MATRIX_TUNE" = "on" ]; then
                configure_args="$configure_args --with-base-optimization=tuned"
              else
                configure_args="$configure_args --with-base-optimization=default"
              fi

              tarball="$(ls dist/*.tar.xz | head -n1)"
              srcdir="$(tar -tf "$tarball" 2>/dev/null | head -n1 | cut -d/ -f1)"
              tar -xf "$tarball" -C dist
              cd "dist/$srcdir"
              ./configure $configure_args
              jobs="$(getconf _NPROCESSORS_ONLN 2>/dev/null || nproc 2>/dev/null || echo 2)"
              make -j"$jobs"
              make check
            '


  build-linux-x86-32:
    runs-on: ubuntu-24.04
    needs: package
    strategy:
      fail-fast: false
      matrix:
        x86_variant: [i386]
        compiler: [gcc, clang, tcc]
        lto: ['on', 'off']
        tune: ['on', 'off']
        exclude:
          - compiler: tcc
            lto: 'on'
          - compiler: tcc
            tune: 'on'
    name: linux-x86-32 (debian-bullseye, ${{ matrix.x86_variant }}, ${{ matrix.compiler }}, lto-${{ matrix.lto }}, tune-${{ matrix.tune }})

    steps:
      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: c-arbint-dist
          path: dist

      - name: Build and test (linux/386 Docker)
        run: |
          mkdir -p artifacts
          docker run --rm \
            --platform linux/386 \
            -e X86_VARIANT="${{ matrix.x86_variant }}" \
            -e CC="${{ matrix.compiler }}" \
            -e MATRIX_LTO="${{ matrix.lto }}" \
            -e MATRIX_TUNE="${{ matrix.tune }}" \
            -v "$PWD/dist:/work/dist" \
            -v "$PWD/artifacts:/work/artifacts" \
            -w /work \
            debian:bullseye-slim \
            sh -eu -c '
              export DEBIAN_FRONTEND=noninteractive
              apt-get -o Acquire::Retries=5 update
              apt-get -o Acquire::Retries=5 install -y --no-install-recommends \
                build-essential clang lld tcc pkg-config tar xz-utils groff

              if [ "$X86_VARIANT" = "i686" ]; then
                march_value="pentium"
              else
                march_value="i386"
              fi

              configure_args=""
              if [ "$MATRIX_LTO" = "on" ]; then
                configure_args="$configure_args --enable-lto=yes"
              else
                configure_args="$configure_args --enable-lto=no"
              fi

              if [ "$MATRIX_TUNE" = "on" ]; then
                configure_args="$configure_args --with-base-optimization=tuned --with-march=$march_value"
              else
                configure_args="$configure_args --with-base-optimization=default"
              fi

              if [ "$CC" = "clang" ] && [ "$MATRIX_LTO" = "on" ]; then
                export LDFLAGS="${LDFLAGS:-} -fuse-ld=lld"
              fi

              tarball="$(ls dist/*.tar.xz | head -n1)"
              srcdir="$(tar -tf "$tarball" 2>/dev/null | head -n1 | cut -d/ -f1)"
              tar -xf "$tarball" -C dist
              cd "dist/$srcdir"
              ./configure $configure_args
              jobs="$(getconf _NPROCESSORS_ONLN 2>/dev/null || nproc 2>/dev/null || echo 2)"
              make -j"$jobs"
              make check
              echo "=== Contents of src/.libs ==="
              ls -la src/.libs/ || true

              # Copy shared libraries to artifacts directory
              find src/.libs -name "*.so*" -type f -exec cp {} /work/artifacts/ \;
            '

      - name: Upload shared library artifacts
        if: matrix.compiler != 'tcc' && matrix.tune == 'off'
        uses: actions/upload-artifact@v4
        with:
          name: build-linux-x86-32-${{ matrix.x86_variant }}-${{ matrix.compiler }}-lto${{ matrix.lto }}
          path: |
            artifacts/*.so
            artifacts/*.so.*
          if-no-files-found: warn

  build-windows-mingw-64:
    runs-on: windows-latest
    needs: package
    strategy:
      fail-fast: false
      matrix:
        compiler: [gcc, tcc]
        lto: ['on', 'off']
        tune: ['on', 'off']
        exclude:
          - compiler: tcc
            lto: 'on'
          - compiler: tcc
            tune: 'on'
    name: build-windows-mingw (x86_64, ${{ matrix.compiler }}, lto-${{ matrix.lto }}, tune-${{ matrix.tune }})

    steps:
      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            make
            tar
            xz
            curl
            groff
            pkgconf
            mingw-w64-x86_64-gcc

      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: c-arbint-dist
          path: dist

      - name: Extract, build, and test
        shell: msys2 {0}
        run: |
          set -euo pipefail

          arch="x86_64"
          export CC="${{ matrix.compiler }}"

          configure_args="--disable-dependency-tracking"
          if [ "${{ matrix.lto }}" = "on" ]; then
            configure_args="$configure_args --enable-lto=yes"
          else
            configure_args="$configure_args --enable-lto=no"
          fi

          if [ "${{ matrix.tune }}" = "on" ]; then
            configure_args="$configure_args --with-base-optimization=tuned"
          else
            configure_args="$configure_args --with-base-optimization=default"
          fi

          tarball="$(ls dist/*.tar.xz | sed -n '1p')"
          srcdir="$(tar -tf "$tarball" 2>/dev/null | sed -n '1p' | cut -d/ -f1)"
          tar -xf "$tarball" -C dist
          cd "dist/$srcdir"
          jobs="$(nproc 2>/dev/null || echo 2)"

          if [ "${{ matrix.compiler }}" = "tcc" ]; then
            tcc_root="$PWD/.tinycc-$arch"
            tcc_src_dir="$tcc_root/tcc-0.9.27"
            tcc_prefix="$tcc_root/prefix"
            rm -rf "$tcc_root"
            mkdir -p "$tcc_root"
            tcc_archive="$tcc_root/tcc-0.9.27.tar.bz2"
            tcc_url_primary="https://download-mirror.savannah.gnu.org/releases/tinycc/tcc-0.9.27.tar.bz2"
            tcc_url_fallback="https://download-mirror.savannah.nongnu.org/releases/tinycc/tcc-0.9.27.tar.bz2"
            if ! curl -fsSL --retry 3 --retry-delay 2 -o "$tcc_archive" "$tcc_url_primary"; then
              curl -fsSL --retry 3 --retry-delay 2 -o "$tcc_archive" "$tcc_url_fallback"
            fi
            echo "de23af78fca90ce32dff2dd45b3432b2334740bb9bb7b05bf60fdbfc396ceb9c  $tcc_archive" | sha256sum -c -
            tar -xf "$tcc_archive" -C "$tcc_root"
            cd "$tcc_src_dir"
            tcc_cpu="x86_64"
            ./configure --prefix="$tcc_prefix" --cpu="$tcc_cpu" --config-mingw32
            make -j"$jobs"
            make install
            export PATH="$tcc_prefix/bin:$tcc_src_dir:$PATH"
            cd ../..

            tcc_bin=""
            if [ -x "$tcc_prefix/bin/tcc.exe" ]; then
              tcc_bin="$tcc_prefix/bin/tcc.exe"
            elif [ -x "$tcc_prefix/bin/tcc" ]; then
              tcc_bin="$tcc_prefix/bin/tcc"
            elif [ -x "$tcc_src_dir/tcc.exe" ]; then
              tcc_bin="$tcc_src_dir/tcc.exe"
            elif [ -x "$tcc_src_dir/tcc" ]; then
              tcc_bin="$tcc_src_dir/tcc"
            fi

            if [ -z "$tcc_bin" ]; then
              echo "Unable to locate tcc executable after TinyCC install." >&2
              exit 1
            fi

            tcc_runtime="$(find "$tcc_root" -type f -name 'libtcc1.a' | sed -n '1p')"
            if [ -z "$tcc_runtime" ]; then
              echo "Unable to locate libtcc1.a after TinyCC install." >&2
              exit 1
            fi
            tcc_libdir="$(dirname "$tcc_runtime")"
            tcc_runtime_root="$(dirname "$tcc_libdir")"
            tcc_base_root="$tcc_runtime_root"
            case "$tcc_libdir" in
              */lib/tcc)
                tcc_base_root="$(dirname "$(dirname "$tcc_libdir")")"
                ;;
              */lib)
                tcc_base_root="$(dirname "$tcc_libdir")"
                ;;
            esac
            tcc_base_lib="$tcc_base_root/lib"
            echo "Using libtcc1 runtime: $tcc_runtime"
            echo "Using TinyCC base root: $tcc_base_root"
            tcc_wrapper="$tcc_root/tcc-driver.sh"
            printf '%s\n' '#!/usr/bin/env bash' \
              'set -euo pipefail' \
              'args=()' \
              'while [ "$#" -gt 0 ]; do' \
              '  if [ "$1" = "-link" ] && [ "${2-}" = "-dll" ]; then' \
              '    args+=("-shared")' \
              '    shift 2' \
              '    continue' \
              '  fi' \
              '  args+=("$1")' \
              '  shift' \
              'done' \
              "exec \"$tcc_bin\" -B\"$tcc_base_root\" -L\"$tcc_base_root\" -L\"$tcc_base_lib\" -L\"$tcc_libdir\" \"\${args[@]}\"" \
              > "$tcc_wrapper"
            chmod +x "$tcc_wrapper"
            export CC="$tcc_wrapper"
            tcc_internal_cppflags=""
            if [ -d "$tcc_libdir/include" ]; then
              tcc_internal_cppflags="$tcc_internal_cppflags -I$tcc_libdir/include"
            fi
            if [ -d "$tcc_base_root/include" ]; then
              tcc_internal_cppflags="$tcc_internal_cppflags -I$tcc_base_root/include"
            fi
            export CPPFLAGS="${CPPFLAGS:-}$tcc_internal_cppflags"
            export LDFLAGS="${LDFLAGS:-} -L$tcc_base_root -L$tcc_base_lib -L$tcc_libdir"
            tcc_include_path=""
            if [ -d "$tcc_base_root/include" ]; then
              tcc_include_path="$tcc_base_root/include"
            fi
            if [ -d "$tcc_libdir/include" ]; then
              if [ -n "$tcc_include_path" ]; then
                tcc_include_path="$tcc_include_path:$tcc_libdir/include"
              else
                tcc_include_path="$tcc_libdir/include"
              fi
            fi
            if [ -n "$tcc_include_path" ]; then
              export C_INCLUDE_PATH="$tcc_include_path${C_INCLUDE_PATH:+:$C_INCLUDE_PATH}"
            fi
            export LIBRARY_PATH="$tcc_base_root:$tcc_base_lib:$tcc_libdir${LIBRARY_PATH:+:$LIBRARY_PATH}"
          fi

          dump_logs_on_failure() {
            rc="$?"
            if [ "$rc" -ne 0 ]; then
              set +e
              for f in configure.log make.log check.log config.log config.status tests/test-suite.log; do
                if [ -f "$f" ]; then
                  echo "===== $f ====="
                  tail -n 300 "$f" || true
                fi
              done
            fi
          }
          trap dump_logs_on_failure EXIT

          "$CC" -v || true
          ./configure $configure_args 2>&1 | tee configure.log
          make -j"$jobs" 2>&1 | tee make.log
          echo "=== Contents of src/.libs ==="
          ls -la src/.libs/ || true
          if [ "${{ matrix.compiler }}" = "tcc" ]; then
            dll_path="$(ls src/.libs/libarbint-*.dll 2>/dev/null | sed -n '1p')"
            if [ -n "$dll_path" ] && [ ! -f src/.libs/libarbint.lib ]; then
              cp "$dll_path" src/.libs/libarbint.lib
            fi

            # Helper to extract check_PROGRAMS from a Makefile
            extract_check_programs() {
              awk '
                BEGIN { collecting=0; line="" }
                /^check_PROGRAMS[[:space:]]*=/ {
                  collecting=1
                  sub(/^[^=]*=[[:space:]]*/, "", $0)
                }
                collecting {
                  gsub(/[[:space:]]+$/, "", $0)
                  if (line != "") line = line " "
                  line = line $0
                  if ($0 ~ /\\$/) {
                    sub(/\\$/, "", line)
                    next
                  }
                  print line
                  exit
                }
              ' "$1"
            }

            exeext="$(sed -n 's/^EXEEXT = //p' tests/Makefile | sed -n '1p')"

            # Build and run tests
            test_bins_raw="$(extract_check_programs tests/Makefile)"
            test_bins="$(printf '%s\n' "$test_bins_raw" | awk -v e="$exeext" '{ gsub(/\$\(EXEEXT\)/, e); print }')"
            if [ -z "$test_bins" ]; then
              echo "Unable to resolve \$(check_PROGRAMS) from tests/Makefile" | tee -a check.log
              exit 1
            fi
            make -C tests -j"$jobs" $test_bins 2>&1 | tee check.log
            export PATH="$PWD/src/.libs:$PWD/tests/.libs:$PWD/demos/.libs:$PATH"
            for test_name in $test_bins; do
              test_bin="tests/.libs/${test_name}"
              if [ ! -x "$test_bin" ]; then
                test_bin="tests/.libs/${test_name%.exe}"
              fi
              echo "Running $test_bin" | tee -a check.log
              "$test_bin" 2>&1 | tee -a check.log
            done

            # Build and run demos
            demo_bins_raw="$(extract_check_programs demos/Makefile)"
            demo_bins="$(printf '%s\n' "$demo_bins_raw" | awk -v e="$exeext" '{ gsub(/\$\(EXEEXT\)/, e); print }')"
            if [ -n "$demo_bins" ]; then
              make -C demos -j"$jobs" $demo_bins 2>&1 | tee -a check.log
              for demo_name in $demo_bins; do
                demo_bin="demos/.libs/${demo_name}"
                if [ ! -x "$demo_bin" ]; then
                  demo_bin="demos/.libs/${demo_name%.exe}"
                fi
                echo "Running $demo_bin" | tee -a check.log
                "$demo_bin" 2>&1 | tee -a check.log
              done
            fi
          else
            make check 2>&1 | tee check.log
          fi

      - name: Copy artifacts to upload directory
        if: matrix.compiler != 'tcc' && matrix.tune == 'off'
        shell: msys2 {0}
        run: |
          mkdir -p artifacts
          find dist -name "*.dll" | xargs -I {} cp {} artifacts/ || true
          ls -la artifacts/

      - name: Upload DLL artifacts
        if: matrix.compiler != 'tcc' && matrix.tune == 'off'
        uses: actions/upload-artifact@v4
        with:
          name: build-windows-mingw-x86_64-${{ matrix.compiler }}-lto${{ matrix.lto }}
          path: artifacts/*
          if-no-files-found: warn

  build-windows-mingw-32:
    runs-on: windows-latest
    needs: package
    strategy:
      fail-fast: false
      matrix:
        compiler: [gcc, tcc]
        lto: ['on', 'off']
        tune: ['on', 'off']
        exclude:
          - compiler: tcc
            lto: 'on'
          - compiler: tcc
            tune: 'on'
    name: build-windows-mingw (x86, ${{ matrix.compiler }}, lto-${{ matrix.lto }}, tune-${{ matrix.tune }})

    steps:
      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW32
          update: true
          install: >-
            make
            tar
            xz
            curl
            groff
            pkgconf
            mingw-w64-i686-gcc

      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: c-arbint-dist
          path: dist

      - name: Extract, build, and test
        shell: msys2 {0}
        run: |
          set -euo pipefail

          arch="x86"
          export CC="${{ matrix.compiler }}"

          configure_args="--disable-dependency-tracking"
          if [ "${{ matrix.lto }}" = "on" ]; then
            configure_args="$configure_args --enable-lto=yes"
          else
            configure_args="$configure_args --enable-lto=no"
          fi

          if [ "${{ matrix.tune }}" = "on" ]; then
            configure_args="$configure_args --with-base-optimization=tuned"
          else
            configure_args="$configure_args --with-base-optimization=default"
          fi

          tarball="$(ls dist/*.tar.xz | sed -n '1p')"
          srcdir="$(tar -tf "$tarball" 2>/dev/null | sed -n '1p' | cut -d/ -f1)"
          tar -xf "$tarball" -C dist
          cd "dist/$srcdir"
          jobs="$(nproc 2>/dev/null || echo 2)"

          if [ "${{ matrix.compiler }}" = "tcc" ]; then
            tcc_root="$PWD/.tinycc-$arch"
            tcc_src_dir="$tcc_root/tcc-0.9.27"
            tcc_prefix="$tcc_root/prefix"
            rm -rf "$tcc_root"
            mkdir -p "$tcc_root"
            tcc_archive="$tcc_root/tcc-0.9.27.tar.bz2"
            tcc_url_primary="https://download-mirror.savannah.gnu.org/releases/tinycc/tcc-0.9.27.tar.bz2"
            tcc_url_fallback="https://download-mirror.savannah.nongnu.org/releases/tinycc/tcc-0.9.27.tar.bz2"
            if ! curl -fsSL --retry 3 --retry-delay 2 -o "$tcc_archive" "$tcc_url_primary"; then
              curl -fsSL --retry 3 --retry-delay 2 -o "$tcc_archive" "$tcc_url_fallback"
            fi
            echo "de23af78fca90ce32dff2dd45b3432b2334740bb9bb7b05bf60fdbfc396ceb9c  $tcc_archive" | sha256sum -c -
            tar -xf "$tcc_archive" -C "$tcc_root"
            cd "$tcc_src_dir"
            tcc_cpu="i386"
            ./configure --prefix="$tcc_prefix" --cpu="$tcc_cpu" --config-mingw32
            make -j"$jobs"
            make install
            export PATH="$tcc_prefix/bin:$tcc_src_dir:$PATH"
            cd ../..

            tcc_bin=""
            if [ -x "$tcc_prefix/bin/tcc.exe" ]; then
              tcc_bin="$tcc_prefix/bin/tcc.exe"
            elif [ -x "$tcc_prefix/bin/tcc" ]; then
              tcc_bin="$tcc_prefix/bin/tcc"
            elif [ -x "$tcc_src_dir/tcc.exe" ]; then
              tcc_bin="$tcc_src_dir/tcc.exe"
            elif [ -x "$tcc_src_dir/tcc" ]; then
              tcc_bin="$tcc_src_dir/tcc"
            fi

            if [ -z "$tcc_bin" ]; then
              echo "Unable to locate tcc executable after TinyCC install." >&2
              exit 1
            fi

            tcc_runtime="$(find "$tcc_root" -type f -name 'libtcc1.a' | sed -n '1p')"
            if [ -z "$tcc_runtime" ]; then
              echo "Unable to locate libtcc1.a after TinyCC install." >&2
              exit 1
            fi
            tcc_libdir="$(dirname "$tcc_runtime")"
            tcc_runtime_root="$(dirname "$tcc_libdir")"
            tcc_base_root="$tcc_runtime_root"
            case "$tcc_libdir" in
              */lib/tcc)
                tcc_base_root="$(dirname "$(dirname "$tcc_libdir")")"
                ;;
              */lib)
                tcc_base_root="$(dirname "$tcc_libdir")"
                ;;
            esac
            tcc_base_lib="$tcc_base_root/lib"
            echo "Using libtcc1 runtime: $tcc_runtime"
            echo "Using TinyCC base root: $tcc_base_root"
            tcc_wrapper="$tcc_root/tcc-driver.sh"
            printf '%s\n' '#!/usr/bin/env bash' \
              'set -euo pipefail' \
              'args=()' \
              'while [ "$#" -gt 0 ]; do' \
              '  if [ "$1" = "-link" ] && [ "${2-}" = "-dll" ]; then' \
              '    args+=("-shared")' \
              '    shift 2' \
              '    continue' \
              '  fi' \
              '  args+=("$1")' \
              '  shift' \
              'done' \
              "exec \"$tcc_bin\" -B\"$tcc_base_root\" -L\"$tcc_base_root\" -L\"$tcc_base_lib\" -L\"$tcc_libdir\" \"\${args[@]}\"" \
              > "$tcc_wrapper"
            chmod +x "$tcc_wrapper"
            export CC="$tcc_wrapper"
            tcc_internal_cppflags=""
            if [ -d "$tcc_libdir/include" ]; then
              tcc_internal_cppflags="$tcc_internal_cppflags -I$tcc_libdir/include"
            fi
            if [ -d "$tcc_base_root/include" ]; then
              tcc_internal_cppflags="$tcc_internal_cppflags -I$tcc_base_root/include"
            fi
            export CPPFLAGS="${CPPFLAGS:-}$tcc_internal_cppflags"
            export LDFLAGS="${LDFLAGS:-} -L$tcc_base_root -L$tcc_base_lib -L$tcc_libdir"
            tcc_include_path=""
            if [ -d "$tcc_base_root/include" ]; then
              tcc_include_path="$tcc_base_root/include"
            fi
            if [ -d "$tcc_libdir/include" ]; then
              if [ -n "$tcc_include_path" ]; then
                tcc_include_path="$tcc_include_path:$tcc_libdir/include"
              else
                tcc_include_path="$tcc_libdir/include"
              fi
            fi
            if [ -n "$tcc_include_path" ]; then
              export C_INCLUDE_PATH="$tcc_include_path${C_INCLUDE_PATH:+:$C_INCLUDE_PATH}"
            fi
            export LIBRARY_PATH="$tcc_base_root:$tcc_base_lib:$tcc_libdir${LIBRARY_PATH:+:$LIBRARY_PATH}"
          fi

          dump_logs_on_failure() {
            rc="$?"
            if [ "$rc" -ne 0 ]; then
              set +e
              for f in configure.log make.log check.log config.log config.status tests/test-suite.log; do
                if [ -f "$f" ]; then
                  echo "===== $f ====="
                  tail -n 300 "$f" || true
                fi
              done
            fi
          }
          trap dump_logs_on_failure EXIT

          "$CC" -v || true
          ./configure $configure_args 2>&1 | tee configure.log
          make -j"$jobs" 2>&1 | tee make.log
          echo "=== Contents of src/.libs ==="
          ls -la src/.libs/ || true
          if [ "${{ matrix.compiler }}" = "tcc" ]; then
            dll_path="$(ls src/.libs/libarbint-*.dll 2>/dev/null | sed -n '1p')"
            if [ -n "$dll_path" ] && [ ! -f src/.libs/libarbint.lib ]; then
              cp "$dll_path" src/.libs/libarbint.lib
            fi

            # Helper to extract check_PROGRAMS from a Makefile
            extract_check_programs() {
              awk '
                BEGIN { collecting=0; line="" }
                /^check_PROGRAMS[[:space:]]*=/ {
                  collecting=1
                  sub(/^[^=]*=[[:space:]]*/, "", $0)
                }
                collecting {
                  gsub(/[[:space:]]+$/, "", $0)
                  if (line != "") line = line " "
                  line = line $0
                  if ($0 ~ /\\$/) {
                    sub(/\\$/, "", line)
                    next
                  }
                  print line
                  exit
                }
              ' "$1"
            }

            exeext="$(sed -n 's/^EXEEXT = //p' tests/Makefile | sed -n '1p')"

            # Build and run tests
            test_bins_raw="$(extract_check_programs tests/Makefile)"
            test_bins="$(printf '%s\n' "$test_bins_raw" | awk -v e="$exeext" '{ gsub(/\$\(EXEEXT\)/, e); print }')"
            if [ -z "$test_bins" ]; then
              echo "Unable to resolve \$(check_PROGRAMS) from tests/Makefile" | tee -a check.log
              exit 1
            fi
            make -C tests -j"$jobs" $test_bins 2>&1 | tee check.log
            export PATH="$PWD/src/.libs:$PWD/tests/.libs:$PWD/demos/.libs:$PATH"
            for test_name in $test_bins; do
              test_bin="tests/.libs/${test_name}"
              if [ ! -x "$test_bin" ]; then
                test_bin="tests/.libs/${test_name%.exe}"
              fi
              echo "Running $test_bin" | tee -a check.log
              "$test_bin" 2>&1 | tee -a check.log
            done

            # Build and run demos
            demo_bins_raw="$(extract_check_programs demos/Makefile)"
            demo_bins="$(printf '%s\n' "$demo_bins_raw" | awk -v e="$exeext" '{ gsub(/\$\(EXEEXT\)/, e); print }')"
            if [ -n "$demo_bins" ]; then
              make -C demos -j"$jobs" $demo_bins 2>&1 | tee -a check.log
              for demo_name in $demo_bins; do
                demo_bin="demos/.libs/${demo_name}"
                if [ ! -x "$demo_bin" ]; then
                  demo_bin="demos/.libs/${demo_name%.exe}"
                fi
                echo "Running $demo_bin" | tee -a check.log
                "$demo_bin" 2>&1 | tee -a check.log
              done
            fi
          else
            make check 2>&1 | tee check.log
          fi

      - name: Copy artifacts to upload directory
        if: matrix.compiler != 'tcc' && matrix.tune == 'off'
        shell: msys2 {0}
        run: |
          mkdir -p artifacts
          find dist -name "*.dll" | xargs -I {} cp {} artifacts/ || true
          ls -la artifacts/

      - name: Upload DLL artifacts
        if: matrix.compiler != 'tcc' && matrix.tune == 'off'
        uses: actions/upload-artifact@v4
        with:
          name: build-windows-mingw-x86-${{ matrix.compiler }}-lto${{ matrix.lto }}
          path: artifacts/*
          if-no-files-found: warn

  build-windows-msvc:
    runs-on: windows-latest
    needs: package
    strategy:
      fail-fast: false
      matrix:
        arch: [x64, x86]
        lto: ['on']
    name: build-windows-msvc (${{ matrix.arch }}, lto-${{ matrix.lto }})

    steps:
      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: ${{ matrix.arch }}

      - name: Setup MSYS2 tools
        uses: msys2/setup-msys2@v2
        with:
          msystem: MSYS
          update: true
          install: >-
            make
            tar
            xz
            groff
            pkgconf

      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: c-arbint-dist
          path: dist

      - name: Extract, build, and test (MSVC)
        shell: msys2 {0}
        env:
          MSYS2_PATH_TYPE: inherit
          MSYS2_ARG_CONV_EXCL: '*'
        run: |
          set -euo pipefail

          export CC=cl
          export CL="${CL:-} -nologo"

          configure_args="--disable-dependency-tracking --with-base-optimization=default --enable-shared --disable-static"
          if [ "${{ matrix.lto }}" = "off" ]; then
            configure_args="$configure_args --enable-lto=no"
          else
            configure_args="$configure_args --enable-lto=auto"
          fi
          configure_args="$configure_args --enable-warnings"

          tarball="$(ls dist/*.tar.xz | sed -n '1p')"
          srcdir="$(tar -tf "$tarball" 2>/dev/null | sed -n '1p' | cut -d/ -f1)"
          tar -xf "$tarball" -C dist
          cd "dist/$srcdir"
          jobs="$(nproc 2>/dev/null || echo 2)"

          dump_logs_on_failure() {
            rc="$?"
            if [ "$rc" -ne 0 ]; then
              set +e
              for f in configure.log make.log check.log config.log config.status tests/test-suite.log; do
                if [ -f "$f" ]; then
                  echo "===== $f ====="
                  tail -n 300 "$f" || true
                fi
              done
            fi
          }
          trap dump_logs_on_failure EXIT

          command -v "$CC" >/dev/null 2>&1 || {
            echo "MSVC compiler '$CC' not found on PATH" >&2
            exit 1
          }

          "$CC" -Bv || true
          if ! ./configure $configure_args 2>&1 | tee configure.log; then
            if [ -f config.log ]; then
              echo "===== config.log ====="
              tail -n 500 config.log || true
            fi
            exit 1
          fi
          make -j"$jobs" 2>&1 | tee make.log
          make check 2>&1 | tee check.log
          echo "=== Contents of src/.libs ==="
          ls -la src/.libs/ || true

      - name: Copy artifacts to upload directory
        shell: msys2 {0}
        run: |
          mkdir -p artifacts
          find dist -name "*.dll" | xargs -I {} cp {} artifacts/ || true
          ls -la artifacts/

      - name: Upload DLL artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-windows-msvc-${{ matrix.arch }}-lto${{ matrix.lto }}
          path: artifacts/*
          if-no-files-found: warn

  build-exotic-arch:
    runs-on: ubuntu-24.04
    needs: package
    strategy:
      fail-fast: false
      matrix:
        include:
          # ppc64le - IBM POWER little-endian
          - arch: ppc64le
            platform: linux/ppc64le
            image: debian:bookworm-slim
            image_tag: debian-bookworm
            compiler: clang
          - arch: ppc64le
            platform: linux/ppc64le
            image: alpine:latest
            image_tag: alpine
            compiler: gcc
          # s390x - IBM Z mainframe (big-endian)
          - arch: s390x
            platform: linux/s390x
            image: debian:bookworm-slim
            image_tag: debian-bookworm
            compiler: clang
          - arch: s390x
            platform: linux/s390x
            image: alpine:latest
            image_tag: alpine
            compiler: gcc
          # riscv64 - RISC-V 64-bit (only unstable images available)
          - arch: riscv64
            platform: linux/riscv64
            image: debian:sid-slim
            image_tag: debian-sid
            compiler: gcc
          - arch: riscv64
            platform: linux/riscv64
            image: alpine:edge
            image_tag: alpine-edge
            compiler: gcc
          # mips64le - MIPS 64-bit little-endian
          - arch: mips64le
            platform: linux/mips64le
            image: debian:bookworm-slim
            image_tag: debian-bookworm
            compiler: gcc
          - arch: mips64le
            platform: linux/mips64le
            image: debian:bookworm-slim
            image_tag: debian-bookworm
            compiler: clang
            # Clang on Debian mips64le passes wrong linker emulation (-m elf instead of -m elf64ltsmip)
            ldflags: "-Wl,-m,elf64ltsmip"
          # arm32v7 - ARMv7 32-bit (Raspberry Pi 2/3/4 in 32-bit mode)
          - arch: arm32v7
            platform: linux/arm/v7
            image: debian:bookworm-slim
            image_tag: debian-bookworm
            compiler: gcc
          - arch: arm32v7
            platform: linux/arm/v7
            image: alpine:latest
            image_tag: alpine
            compiler: gcc
          # arm32v5 - ARMv5TE (old embedded ARM)
          - arch: arm32v5
            platform: linux/arm/v5
            image: debian:bookworm-slim
            image_tag: debian-bookworm
            compiler: gcc
    name: build-exotic (${{ matrix.arch }}, ${{ matrix.image_tag }}, ${{ matrix.compiler }})

    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: c-arbint-dist
          path: dist

      - name: Build and test in Docker (${{ matrix.platform }})
        run: |
          mkdir -p artifacts
          docker run --rm \
            --platform ${{ matrix.platform }} \
            -e CC="${{ matrix.compiler }}" \
            -e EXTRA_LDFLAGS="${{ matrix.ldflags }}" \
            -v "$PWD/dist:/work/dist" \
            -v "$PWD/artifacts:/work/artifacts" \
            -w /work \
            "${{ matrix.image }}" \
            sh -eu -c '
              # Detect distro and install dependencies
              if [ -f /etc/alpine-release ]; then
                pkgs="build-base pkgconf tar xz groff"
                if [ "$CC" = "clang" ]; then
                  pkgs="$pkgs clang"
                fi
                apk add --no-cache $pkgs
              elif [ -f /etc/debian_version ]; then
                export DEBIAN_FRONTEND=noninteractive
                apt-get -o Acquire::Retries=5 update
                pkgs="build-essential pkg-config tar xz-utils groff"
                if [ "$CC" = "clang" ]; then
                  pkgs="$pkgs clang"
                fi
                apt-get -o Acquire::Retries=5 install -y --no-install-recommends $pkgs
              else
                echo "Unknown distro" >&2
                exit 1
              fi

              tarball="$(ls dist/*.tar.xz | head -n1)"
              srcdir="$(tar -tf "$tarball" 2>/dev/null | head -n1 | cut -d/ -f1)"
              tar -xf "$tarball" -C dist
              cd "dist/$srcdir"

              # Apply extra LDFLAGS if specified (e.g., for mips64le clang linker emulation fix)
              configure_ldflags=""
              if [ -n "${EXTRA_LDFLAGS:-}" ]; then
                configure_ldflags="LDFLAGS=$EXTRA_LDFLAGS"
              fi

              ./configure --enable-shared --disable-static --enable-lto=no $configure_ldflags
              jobs="$(getconf _NPROCESSORS_ONLN 2>/dev/null || nproc 2>/dev/null || echo 2)"
              make -j"$jobs"
              make check
              echo "=== Contents of src/.libs ==="
              ls -la src/.libs/ || true

              # Copy shared libraries to artifacts directory
              find src/.libs -name "*.so*" -type f -exec cp {} /work/artifacts/ \; || true
            '

      - name: Upload shared library artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-exotic-${{ matrix.arch }}-${{ matrix.compiler }}-${{ matrix.image_tag }}
          path: artifacts/*
          if-no-files-found: warn

  # Exotic architectures from debian-ports that require special QEMU setup.
  # These use our own pre-built images from ghcr.io (built by build-docker-images.yml).
  # The images are single-arch and contain build-essential pre-installed.
  build-debian-ports:
    runs-on: ubuntu-24.04
    needs: package
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: ppc64
            qemu_arch: ppc64
          - arch: powerpc
            qemu_arch: ppc
          - arch: hppa
            qemu_arch: hppa
          - arch: m68k
            qemu_arch: m68k
          - arch: sh4
            qemu_arch: sh4
          - arch: alpha
            qemu_arch: alpha
    name: build-debian-ports (${{ matrix.arch }})

    steps:
      - name: Install QEMU user-mode emulation
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-user-static binfmt-support

          # Register all QEMU user-mode handlers
          sudo update-binfmts --enable

          # Verify the target architecture is registered
          echo "=== Registered binfmt handlers ==="
          ls -la /proc/sys/fs/binfmt_misc/ || true
          cat /proc/sys/fs/binfmt_misc/qemu-${{ matrix.qemu_arch }} 2>/dev/null || echo "Handler for ${{ matrix.qemu_arch }} not found"

      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: c-arbint-dist
          path: dist

      - name: Build and test in Docker
        run: |
          mkdir -p artifacts

          # Use our own pre-built image from ghcr.io
          image="ghcr.io/${{ github.repository_owner }}/c-arbint-build-${{ matrix.arch }}:latest"

          # Pull the image (no --platform flag for single-arch images)
          docker pull "$image"

          # Run with QEMU user-mode emulation via binfmt_misc
          # The -v for qemu-user-static makes QEMU available inside the container
          docker run --rm \
            -v /usr/bin/qemu-${{ matrix.qemu_arch }}-static:/usr/bin/qemu-${{ matrix.qemu_arch }}-static:ro \
            -e CC=gcc \
            -v "$PWD/dist:/work/dist" \
            -v "$PWD/artifacts:/work/artifacts" \
            -w /work \
            "$image" \
            sh -eu -c '
              # Build tools are pre-installed in our images
              tarball="$(ls dist/*.tar.xz | head -n1)"
              srcdir="$(tar -tf "$tarball" 2>/dev/null | head -n1 | cut -d/ -f1)"
              tar -xf "$tarball" -C dist
              cd "dist/$srcdir"
              ./configure --enable-shared --disable-static --enable-lto=no
              jobs="$(getconf _NPROCESSORS_ONLN 2>/dev/null || nproc 2>/dev/null || echo 2)"
              make -j"$jobs"
              make check
              echo "=== Contents of src/.libs ==="
              ls -la src/.libs/ || true

              # Copy shared libraries to artifacts directory
              find src/.libs -name "*.so*" -type f -exec cp {} /work/artifacts/ \; || true
            '

      - name: Upload shared library artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-debian-ports-${{ matrix.arch }}-gcc
          path: artifacts/*
          if-no-files-found: warn

  build-macos:
    needs: package
    strategy:
      fail-fast: false
      matrix:
        lto: ['on', 'off']
        tune: ['on', 'off']
    runs-on: macos-15
    name: build-macos (arm64, lto-${{ matrix.lto }}, tune-${{ matrix.tune }})

    steps:
      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: c-arbint-dist
          path: dist

      - name: Extract, build, and test
        run: |
          export CC=clang

          configure_args=""
          if [ "${{ matrix.lto }}" = "on" ]; then
            configure_args="$configure_args --enable-lto=yes"
          else
            configure_args="$configure_args --enable-lto=no"
          fi

          if [ "${{ matrix.tune }}" = "on" ]; then
            configure_args="$configure_args --with-base-optimization=tuned"
          else
            configure_args="$configure_args --with-base-optimization=default"
          fi

          tarball="$(ls dist/*.tar.xz | head -n1)"
          srcdir="$(tar -tf "$tarball" 2>/dev/null | head -n1 | cut -d/ -f1)"
          tar -xf "$tarball" -C dist
          cd "dist/$srcdir"
          ./configure $configure_args
          jobs="$(getconf _NPROCESSORS_ONLN 2>/dev/null || sysctl -n hw.ncpu 2>/dev/null || echo 3)"
          make -j"$jobs"
          make check
          echo "=== Contents of src/.libs ==="
          ls -la src/.libs/ || true

      - name: Copy artifacts to upload directory
        if: matrix.tune == 'off'
        run: |
          mkdir -p artifacts
          find dist -name "*.dylib" | xargs -I {} cp {} artifacts/ || true
          ls -la artifacts/

      - name: Upload shared library artifacts
        if: matrix.tune == 'off'
        uses: actions/upload-artifact@v4
        with:
          name: build-macos-arm64-lto${{ matrix.lto }}
          path: artifacts/*
          if-no-files-found: warn

  build-wasm:
    runs-on: ubuntu-24.04
    needs: package
    strategy:
      fail-fast: false
      matrix:
        mode: [wasm32, wasm64]
        lto: ['on', 'off']
    name: build-wasm (${{ matrix.mode }}, lto-${{ matrix.lto }})

    steps:
      - name: Install Emscripten
        uses: mymindstorm/setup-emsdk@v14

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          # wasm64 (MEMORY64) requires memory64+table64 support in V8.
          # These proposals are unflagged starting in Node 24 (V8 13.6+).
          node-version: '24'

      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: c-arbint-dist
          path: dist

      - name: Extract, build, and test
        run: |
          tarball="$(ls dist/*.tar.xz | head -n1)"
          srcdir="$(tar -tf "$tarball" 2>/dev/null | head -n1 | cut -d/ -f1)"
          tar -xf "$tarball" -C dist
          cd "dist/$srcdir"

          echo "=== Toolchain versions ==="
          emcc --version | head -1
          node --version

          # Build shared only; configure detects emscripten and sets up
          # libtool patches (SIDE_MODULE), test flags (MAIN_MODULE +
          # NODERAWFS), and test runner (run_wasm_test.sh).
          configure_args="--enable-shared --disable-static"
          if [ "${{ matrix.lto }}" = "on" ]; then
            configure_args="$configure_args --enable-lto=yes"
          else
            configure_args="$configure_args --enable-lto=no"
          fi

          em_cflags=""
          em_ldflags=""
          host_triple="wasm32-unknown-emscripten"
          if [ "${{ matrix.mode }}" = "wasm64" ]; then
            host_triple="wasm64-unknown-emscripten"
            em_cflags="-sMEMORY64"
            em_ldflags="-sMEMORY64"
          fi
          configure_args="$configure_args --host=$host_triple"

          emconfigure ./configure $configure_args \
            CFLAGS="${CFLAGS:-} $em_cflags" \
            LDFLAGS="${LDFLAGS:-} $em_ldflags"
          emmake make -j"$(nproc)"

          echo "=== Library ==="
          file src/libarbint.wasm || true
          ls -la src/libarbint.wasm || true

          echo "=== Running tests ==="
          emmake make check || {
            echo "=== Test suite log ==="
            cat tests/test-suite.log 2>/dev/null || true
            exit 1
          }

      - name: Copy artifacts to upload directory
        run: |
          mkdir -p artifacts
          # The libtool-bypass build places the side module in src/.
          find dist -path "*/src/libarbint.wasm" -type f \
            -exec cp -v {} artifacts/ \;
          echo "=== Artifacts to upload ==="
          ls -la artifacts/ || echo "(empty)"

      - name: Upload library artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-wasm-${{ matrix.mode }}-lto${{ matrix.lto }}
          path: artifacts/*
          if-no-files-found: warn

  repack-artifacts:
    runs-on: ubuntu-24.04
    needs: [build-ubuntu, build-linux-x86-32, build-windows-mingw-64, build-windows-mingw-32, build-windows-msvc, build-exotic-arch, build-debian-ports, build-macos, build-wasm]
    if: always()

    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: all-artifacts
          pattern: build-*

      - name: List downloaded artifacts
        run: |
          echo "=== Downloaded artifacts ==="
          find all-artifacts -type f -ls

      - name: Rename and organize binaries
        run: |
          mkdir -p binaries

          # Function to map artifact names to tuple-compiler format
          rename_binary() {
            local artifact_name="$1"
            local file="$2"
            local filename=$(basename "$file")

            # Determine file extension (handle .so, .so.*, .dylib, .dll, .wasm)
            local ext=""
            if [[ "$filename" =~ \.so(\.[0-9]+)*$ ]]; then
              ext="so"
            elif [[ "$filename" =~ \.dylib$ ]]; then
              ext="dylib"
            elif [[ "$filename" =~ \.dll$ ]]; then
              ext="dll"
            elif [[ "$filename" =~ \.wasm$ ]]; then
              ext="wasm"
            else
              echo "Warning: Unknown file extension: $filename"
              return 1
            fi

            # Extract platform, compiler, and lto status
            local tuple=""
            local compiler=""
            local lto_suffix=""

            # Parse artifact name: build-{platform}-{arch}-{compiler}-lto{on|off}
            # Examples:
            #   build-linux-x86-64-gcc-ltoon -> amd64-linux-lg
            #   build-linux-arm64-clang-ltooff -> arm64-linux-c
            #   build-windows-msvc-x64-ltoauto -> amd64-windows-m

            # Note: x86-64 needs escaping because of the hyphen in the arch name
            if [[ "$artifact_name" =~ ^build-linux-x86-64-([^-]+)-lto(on|off|auto)$ ]]; then
              tuple="amd64-linux"
              compiler="${BASH_REMATCH[1]}"
              lto="${BASH_REMATCH[2]}"
            elif [[ "$artifact_name" =~ ^build-linux-arm64-([^-]+)-lto(on|off|auto)$ ]]; then
              tuple="arm64-linux"
              compiler="${BASH_REMATCH[1]}"
              lto="${BASH_REMATCH[2]}"
            elif [[ "$artifact_name" =~ ^build-linux-x86-32-i386-([^-]+)-lto(on|off|auto)$ ]]; then
              tuple="i386-linux"
              compiler="${BASH_REMATCH[1]}"
              lto="${BASH_REMATCH[2]}"
            # Exotic archs (no LTO, artifact name: build-exotic-{arch}-{compiler}-{image})
            elif [[ "$artifact_name" =~ ^build-exotic-ppc64le-([^-]+)-.+ ]]; then
              tuple="ppc64le-linux"
              compiler="${BASH_REMATCH[1]}"
              lto="off"
            elif [[ "$artifact_name" =~ ^build-exotic-riscv64-([^-]+)-.+ ]]; then
              tuple="rv64gc-linux"
              compiler="${BASH_REMATCH[1]}"
              lto="off"
            elif [[ "$artifact_name" =~ ^build-exotic-s390x-([^-]+)-.+ ]]; then
              tuple="s390x-linux"
              compiler="${BASH_REMATCH[1]}"
              lto="off"
            elif [[ "$artifact_name" =~ ^build-exotic-mips64le-([^-]+)-.+ ]]; then
              tuple="mips64el-linux"
              compiler="${BASH_REMATCH[1]}"
              lto="off"
            elif [[ "$artifact_name" =~ ^build-exotic-arm32v7-([^-]+)-.+ ]]; then
              tuple="armv7-linux"
              compiler="${BASH_REMATCH[1]}"
              lto="off"
            elif [[ "$artifact_name" =~ ^build-exotic-arm32v5-([^-]+)-.+ ]]; then
              tuple="armv5te-linux"
              compiler="${BASH_REMATCH[1]}"
              lto="off"
            # debian-ports architectures (build-debian-ports-{arch}-gcc)
            elif [[ "$artifact_name" =~ ^build-debian-ports-ppc64-gcc$ ]]; then
              tuple="ppc64-linux"
              compiler="gcc"
              lto="off"
            elif [[ "$artifact_name" =~ ^build-debian-ports-powerpc-gcc$ ]]; then
              tuple="ppc-linux"
              compiler="gcc"
              lto="off"
            elif [[ "$artifact_name" =~ ^build-debian-ports-hppa-gcc$ ]]; then
              tuple="hppa-linux"
              compiler="gcc"
              lto="off"
            elif [[ "$artifact_name" =~ ^build-debian-ports-m68k-gcc$ ]]; then
              tuple="m68k-linux"
              compiler="gcc"
              lto="off"
            elif [[ "$artifact_name" =~ ^build-debian-ports-sh4-gcc$ ]]; then
              tuple="sh4-linux"
              compiler="gcc"
              lto="off"
            elif [[ "$artifact_name" =~ ^build-debian-ports-alpha-gcc$ ]]; then
              tuple="alpha-linux"
              compiler="gcc"
              lto="off"
            elif [[ "$artifact_name" =~ ^build-macos-arm64-lto(on|off|auto)$ ]]; then
              tuple="arm64-darwin"
              compiler="clang"
              lto="${BASH_REMATCH[1]}"
            elif [[ "$artifact_name" =~ ^build-windows-mingw-x86_64-([^-]+)-lto(on|off|auto)$ ]]; then
              tuple="amd64-windows"
              compiler="${BASH_REMATCH[1]}"
              lto="${BASH_REMATCH[2]}"
            elif [[ "$artifact_name" =~ ^build-windows-mingw-x86-([^-]+)-lto(on|off|auto)$ ]]; then
              tuple="i386-windows"
              compiler="${BASH_REMATCH[1]}"
              lto="${BASH_REMATCH[2]}"
            elif [[ "$artifact_name" =~ ^build-windows-msvc-x64-lto(on|off|auto)$ ]]; then
              tuple="amd64-windows"
              compiler="msvc"
              lto="${BASH_REMATCH[1]}"
            elif [[ "$artifact_name" =~ ^build-windows-msvc-x86-lto(on|off|auto)$ ]]; then
              tuple="i386-windows"
              compiler="msvc"
              lto="${BASH_REMATCH[1]}"
            elif [[ "$artifact_name" =~ ^build-wasm-(wasm32|wasm64)-lto(on|off)$ ]]; then
              tuple="${BASH_REMATCH[1]}-emscripten"
              compiler="emcc"
              lto="${BASH_REMATCH[2]}"
            else
              echo "Warning: Unable to parse artifact name: $artifact_name"
              return 1
            fi

            # Map compiler names to suffixes
            case "$compiler" in
              gcc) compiler="g" ;;
              clang) compiler="c" ;;
              msvc) compiler="m" ;;
              emcc) compiler="e" ;;
              *) echo "Warning: Unknown compiler: $compiler"; return 1 ;;
            esac

            # Add 'l' prefix for LTO builds
            if [[ "$lto" == "on" || "$lto" == "auto" ]]; then
              lto_suffix="l"
            fi

            # Construct new filename: libarbint-{tuple}-{lto_suffix}{compiler}.{ext}
            local new_name="libarbint-${tuple}-${lto_suffix}${compiler}.${ext}"
            echo "$new_name"
          }

          # Process all binaries
          declare -A file_map  # Map from checksum to chosen filename

          for artifact_dir in all-artifacts/build-*; do
            if [ -d "$artifact_dir" ]; then
              artifact_name=$(basename "$artifact_dir")

              for file in "$artifact_dir"/*; do
                if [ -f "$file" ]; then
                  # Only process library files
                  case "$file" in
                    *.so|*.so.*|*.dylib|*.dll|*.wasm)
                      new_name=$(rename_binary "$artifact_name" "$file")
                      if [ $? -eq 0 ] && [ -n "$new_name" ]; then
                        checksum=$(sha256sum "$file" | awk '{print $1}')

                        # Check for duplicates
                        if [ -n "${file_map[$checksum]:-}" ]; then
                          existing="${file_map[$checksum]}"
                          echo "Duplicate found: $new_name (same as $existing, skipping)"
                        else
                          cp "$file" "binaries/$new_name"
                          file_map[$checksum]="$new_name"
                          echo "Renamed: $artifact_name/$(basename "$file") -> $new_name"
                        fi
                      fi
                      ;;
                  esac
                fi
              done
            fi
          done

          echo "=== Final binaries ==="
          ls -lh binaries/

          # Create xz tarball
          tar -cJf c-arbint-binaries.tar.xz -C binaries .

          echo "=== Created archive ==="
          ls -lh c-arbint-binaries.tar.xz

      - name: Upload repacked archive
        uses: actions/upload-artifact@v4
        with:
          name: c-arbint-binaries-repacked
          path: c-arbint-binaries.tar.xz
          if-no-files-found: error
